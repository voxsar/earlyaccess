{
  "name": "earlyaccess-wishlist",
  "version": "1.0.0",
  "description": "Shopify app for wishlist and early access functionality",
  "scripts": {
    "dev": "shopify app dev",
    "dev:full": "concurrently \"npm run dev:backend\" \"npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate",
    "info": "shopify app info"
  },
  "keywords": [
    "shopify",
    "app",
    "wishlist",
    "early-access"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@shopify/cli": "^3.86.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
